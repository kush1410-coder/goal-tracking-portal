graph TD
    subgraph Frontend Client
        UI["Browser (HTML / CSS / Vanilla JS)"]
    end

    subgraph Backend Server [Node.js & Express]
        API["Express App (server.js)"]
        AuthMiddleware["Auth Middleware (express-session)"]
        Routes["API Routes (Goals, Check-ins, Admin)"]
        Services["Services (Escalation & Reminders)"]
        Cron["Task Scheduler (node-cron)"]
        Notifications["Notification Utility (Email & Teams)"]
    end

    subgraph Database
        SQLite[("SQLite (goal_tracking.db)")]
    end

    UI -- HTTP Requests --> API
    API --> AuthMiddleware
    AuthMiddleware -- Validated --> Routes
    Routes -- Read/Write --> SQLite
    Routes -- Trigger --> Notifications
    
    Cron -- Daily Trigger --> Services
    Services -- Read/Write --> SQLite
    Services -- Send Alerts --> Notifications
