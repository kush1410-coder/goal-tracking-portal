{
  "version": 2,
  "buildCommand": "rm -rf backend/node_modules node_modules && npm install --prefix backend",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.js" },
    { "source": "/(.*)", "destination": "/frontend/$1" }
  ]
}
